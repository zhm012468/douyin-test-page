# 🚀 双面人格测试 H5 — 免费部署上线教程

## 📁 文件说明

```
personality-test/
├── index.html          ← 主文件，所有代码都在这里
├── qrcode.png          ← 你的微信二维码图片（需自行放置）
└── 部署教程.md          ← 本说明文档
```

---

## 一、源码保存 & 本地修改

### 1. 保存源码
所有代码已经包含在 `index.html` 这一个文件中，**无需额外安装任何软件**。把这个文件夹复制到任意位置即可。

### 2. 修改文字内容
用记事本或任意代码编辑器（推荐 [VS Code](https://code.visualstudio.com)）打开 `index.html`：

- **性格描述库**：搜索 `PERSONALITY_DB`（约第 600 行），即可修改所有测试结果文字
- **标题和UI文字**：直接搜索你想改的中文文字即可定位
- **加载文案**：搜索 `LOADING_TEXTS` 修改加载页的趣味提示

### 3. 修改配色
在文件顶部 `:root {}` 区域（约第 25 行），修改 CSS 变量即可一键换色：

```css
:root {
  --gradient-1: #a855f7;   /* 紫色 → 改成你喜欢的颜色 */
  --gradient-2: #ec4899;   /* 粉色 */
  --gradient-3: #6366f1;   /* 蓝色 */
  --gradient-4: #06b6d4;   /* 青色 */
  --bg-deep: #0a0a1a;      /* 背景深色 */
}
```

### 4. 替换微信二维码
1. 将你的微信个人二维码截图保存为 `qrcode.png`
2. 放在 `personality-test/` 目录下
3. 在 `index.html` 中搜索 `qr-placeholder`，将下方的占位 div 替换为：
```html
<img src="qrcode.png" class="qr-real" alt="微信二维码" style="width:120px;height:120px;border-radius:14px;object-fit:cover;">
```

### 5. 替换广告位
搜索 `AD_SLOT` 即可找到所有广告位，共 4 个：
- `AD_SLOT_LEFT`：桌面端左侧（160×600）
- `AD_SLOT_RIGHT`：桌面端右侧（160×600）
- `AD_SLOT_MOBILE_TOP`：移动端顶部（320×50）
- `AD_SLOT_MOBILE_BOTTOM`：移动端底部（320×50）

将广告位内的占位文字替换为你的广告代码或图片即可。

---

## 二、免费无备案部署方案

### 方案A：GitHub Pages（推荐 ⭐⭐⭐⭐⭐）
**完全免费、无需备案、自带 HTTPS**

1. 注册 [GitHub](https://github.com) 账号
2. 新建仓库（Repository），名称随意（如 `personality-test`）
3. 上传 `index.html` 到仓库根目录
4. 进入 `Settings` → `Pages` → Source 选择 `main` 分支 → 点 `Save`
5. 1分钟后，访问 `https://你的用户名.github.io/personality-test/` 即可

> 💡 如果想用自定义域名，在 Pages 设置中填入域名，然后去域名后台添加 CNAME 记录即可。

### 方案B：Cloudflare Pages（推荐 ⭐⭐⭐⭐⭐）
**全球CDN加速、国内访问快、完全免费**

1. 注册 [Cloudflare](https://cloudflare.com) 账号
2. 进入 `Workers & Pages` → `Pages` → `上传资产`
3. 将 `personality-test` 整个文件夹拖入上传
4. 项目名称自定义 → 点击 `部署`
5. 获得 `https://你的项目.pages.dev` 域名

### 方案C：Netlify（推荐 ⭐⭐⭐⭐）
**拖拽部署、自动HTTPS**

1. 打开 [Netlify](https://app.netlify.com)
2. 注册后，直接将 `personality-test` 文件夹拖入页面
3. 自动部署完成，获得 `https://xxx.netlify.app` 域名

### 方案D：Vercel（推荐 ⭐⭐⭐⭐）
1. 注册 [Vercel](https://vercel.com)（用 GitHub 账号直接登录）
2. 导入 GitHub 仓库或直接上传文件
3. 自动部署，获得 `https://xxx.vercel.app` 域名

---

## 三、更易记的短链接（可选）

部署完成后你会获得一个较长的网址，可以用以下工具缩短：

- [短链](https://suol.in) — 免费自选后缀
- [TinyURL](https://tinyurl.com) — 老牌短链服务

---

## 四、微信内打开注意事项

1. 使用 HTTPS 地址（以上方案均自带 HTTPS）
2. 微信内直接打开链接即可
3. 如需在公众号中使用，在公众号菜单设置跳转网页链接
4. 注意：微信可能会拦截部分域名，如遇到拦截可换域名或使用方案B（Cloudflare Pages）

---

## 五、常见问题

**Q: 为什么我改了代码但页面没变？**
A: 浏览器缓存问题，按 `Ctrl+F5` 强制刷新即可。

**Q: 需要后端服务器吗？**
A: 不需要。所有计算都在用户浏览器中完成，无需服务器。

**Q: 用户数据会被收集吗？**
A: 不会。本页面无任何数据上传逻辑，用户输入完全存储在本地浏览器中。

**Q: 能添加支付功能吗？**
A: 可以自行添加微信/支付宝支付JS SDK，但需要注意微信支付的域名备案要求。

**Q: 为什么二维码不显示？**
A: 检查 `qrcode.png` 是否放在 `index.html` 同目录下，文件名是否一致。

---

## 📞 技术支持

如需进一步修改功能或样式，可直接在 `index.html` 中搜索相关中文关键词定位代码位置。
